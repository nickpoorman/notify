extends ../layout
include ../mixins/form-helpers-v3

block content
  .container.bump
    h1= app.username

    form(action='/app/settings', method='put')
      mixin fieldAndLabel({name: 'email', label: 'Username', placeholder: 'Enter username'})
      button(type="submit", class="btn btn-default") Update