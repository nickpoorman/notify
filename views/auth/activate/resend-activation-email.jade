extends ../../layout
include ../../mixins/form-helpers-v3

block content
  .container.bump
    form(action="/auth/resend-activation-email", method="post")
      mixin fieldAndLabel({name: 'email', label: 'Email', placeholder: 'Enter email'})
      button(type="submit", class="btn btn-default") Send